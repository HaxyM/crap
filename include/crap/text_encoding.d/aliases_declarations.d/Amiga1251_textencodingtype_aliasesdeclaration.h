#include "../textencodingtype.h"

#ifndef CRAP_TEXTENCODING_ALIASESDECLARATIONS_AMIGA1251TEXTENCODINGTYPEALIASESDECLARATION
#define CRAP_TEXTENCODING_ALIASESDECLARATIONS_AMIGA1251TEXTENCODINGTYPEALIASESDECLARATION

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'a', 'l', 'i', 'a', 's', 'e', 's'>;

template <class PlaceHolder>
struct alias<PlaceHolder, char, 'a', 'm', 'i', '1', '2', '5', '1'>;

template <class PlaceHolder>
struct alias<PlaceHolder, char, 'a', 'm', 'i', 'g', 'a', '1', '2', '5', '1'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'a', 'n', 'd'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'a', 'r', 'e'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'b', 'e'>;

template <class PlaceHolder>
struct alias<PlaceHolder, char, 'c', 's', 'a', 'm', 'i', 'g', 'a', '1', '2', '5', '1'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'f', 'o', 'r'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'h', 'i', 's', 't', 'o', 'r', 'i', 'c', 'a', 'l'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'm', 'a', 'l', 'y', 's', 'h', 'e', 'v'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'n', 'o', 't'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'p', 'r', 'o', 'v', 'i', 'd', 'e', 'd'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'r', 'e', 'a', 's', 'o', 'n', 's'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 's', 'h', 'o', 'u', 'l', 'd'>;

template <class PlaceHolder> //Looks like bug in libstdc++.
struct alias<PlaceHolder, char, 'u', 's', 'e', 'd'>;
#endif
